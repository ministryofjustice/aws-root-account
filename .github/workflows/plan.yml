name: terraform plan

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

env:
  TF_VAR_sso_identity_store_id: ${{ secrets.SSO_IDENTITY_STORE_ID }}
  TF_VAR_sso_scim_token: ${{ secrets.SSO_SCIM_TOKEN }}
  TF_VAR_sso_tenant_id: ${{ secrets.SSO_TENANT_ID }}

jobs:
  plan:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    defaults:
      run:
        working-directory: ./terraform
    steps:
      - uses: actions/checkout@v3
      - uses: aws-actions/configure-aws-credentials@v1
        with:
          role-to-assume: arn:aws:iam::${{secrets.AWS_ROOT_ACCOUNT_ID}}:role/github-actions
          role-session-name: GitHubActions
          aws-region: eu-west-2
      - uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.3.4
      - run: terraform fmt -check
        continue-on-error: true
      - run: terraform init
      - run: terraform validate -no-color
      - run: terraform plan -no-color
